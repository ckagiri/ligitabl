# Rate limiting — defined at http level, applied in vhost.d/default_location
#
# 10 requests/sec per IP with a burst allowance of 20.
# Returns 429 (not nginx's default 503) so clients know to back off.
limit_req_zone $binary_remote_addr zone=app:10m rate=10r/s;
limit_req_status 429;
