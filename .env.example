# App settings
PORT=8080
JAVA_OPTS=

# Database (used by docker-compose postgres container)
DB_HOST=localhost
DB_NAME=ligitabl
DB_USER=ligitabl
DB_PASSWORD=ligitabl
DB_PORT=5432

# Spring profiles
# For Compose, DB_HOST/DB_PORT are overridden per service (app uses db:5432)
# Liquibase is disabled by default; enable it at runtime with:
#   SPRING_PROFILES_ACTIVE=liquibase

# jOOQ code generation uses DB_* above (no separate JOOQ_* needed)

# ------------------------------
# Test scripts (optional)
# ------------------------------
# These are used by scripts/TestSeeding.sh and scripts/TestAuth.sh.
# For a ready-to-use setup, see .env.test (committed).
#
# DB_CONTAINER=ligitabl-db
# HOST_DB_PORT=55433
#
# BASE_URL=http://localhost:8080
# LOGIN_PATH=/auth/login
# ME_PATH=/api/me
# ADMIN_ONLY_PATH=/api/admin
# PLAYER_ONLY_PATH=/api/player
#
# ADMIN_EMAIL=admin@example.com
# ADMIN_PASSWORD=admin12345
# PLAYER_EMAIL=player1@example.com
# PLAYER_PASSWORD=player12345
# SUPER_EMAIL=superuser@example.com
# SUPER_PASSWORD=super12345
