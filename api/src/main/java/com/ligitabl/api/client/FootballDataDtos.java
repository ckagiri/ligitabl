package com.ligitabl.api.client;

/**
 * Intentionally empty.
 *
 * The Football Data API DTOs used to live in this file as multiple public records/enums.
 * Java requires one public top-level type per file, so they now live in their own files:
 * MatchesResponse, MatchDto, HomeTeam, AwayTeam, Score, FullTime, HalfTime, Winner,
 * Competition, CurrentSeason, CompetitionResponse.
 */
